{
  "name": "filestorage-server-streams-example",
  "version": "0.0.1",
  "main": "index.js",
  "author": "al.kolpak@gmail.com",
  "license": "MIT",
  "scripts": {
    "start": "NODE_ENV=production NODE_PATH=./ ./index.js",
    "dev": "NODE_ENV=development NODE_PATH=./ ./node_modules/.bin/nodemon index.js",
    "test": "NODE_ENV=test NODE_PATH=./ ./node_modules/.bin/mocha --timeout 60000 --exit --recursive test/*.test.js",
    "lint": "./node_modules/.bin/eslint --ext .js ."
  },
  "dependencies": {
    "config": "^3.3.1",
    "mime": "^2.4.4",
    "ramda": "^0.27.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-mocha": "^6.3.0",
    "mocha": "^7.1.1",
    "nodemon": "^2.0.2"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
